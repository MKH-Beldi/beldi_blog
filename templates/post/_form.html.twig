{% form_theme form 'bootstrap_4_layout.html.twig' %}
{{ form_start(form, {attr: {'novalidate':'novalidate'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-lg btn-block btn-primary my-2"><i class="fa fa-save" aria-hidden="true"></i> {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
